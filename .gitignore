browser_demo/.pixi/
browser_demo/out/
demo_env
demo_env_build/.pixi/
demo_env_build/pixi.toml
demo_env_build/pixi.lock
jupyterlite-content/.pixi/
jupyterlite-content/content/
jupyterlite-content/_output/
# Generated by CI (or manually, for local testing) -- never committed, see
# .github/workflows/deploy.yml. site/ only holds hand-written source.
site/assets/
site/jupyterlite/
jupyterlite-content/.jupyterlite.doit.db

# Throwaway local test/validation artifacts (Node harnesses, merged
# kernel_packages copies for swap-testing, etc.) -- never committed, kept
# inside the repo instead of /tmp so they stay alongside the real project.
scratch/
